'use client'

import { withPrivateRoute } from "@/hoc/with-private-route.hoc";

function Analytics() {
  return (
    <>
      <h1>Analytics</h1>
      <section>

      </section>
    </>
  );
}

export default withPrivateRoute(Analytics);
